---
phase: 03-categorization-engine
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified: []
autonomous: false
requirements: [CATG-01, CATG-02, CATG-03, CATG-04, CATG-05, CATG-06, CATG-07, CATG-08]

must_haves:
  truths:
    - "Database schema is pushed to live Supabase with is_system column on rules and seeded data"
    - "Application compiles without TypeScript errors"
    - "All rule evaluation tests pass"
    - "Full categorization flow works end-to-end: upload PDF, create rules, reach 100% categorized, import"
  artifacts: []
  key_links: []
---

<objective>
Push database schema to live Supabase and verify the full categorization engine end-to-end.

Purpose: The schema push is a blocking requirement -- without it, the seeded categories, system PAYMENT rule, and is_system column do not exist in the live database. The checkpoint verifies the complete flow works visually and functionally.

Output: Live database updated, full end-to-end verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-categorization-engine/03-CONTEXT.md
@.planning/phases/03-categorization-engine/03-01-SUMMARY.md
@.planning/phases/03-categorization-engine/03-02-SUMMARY.md
@.planning/phases/03-categorization-engine/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Push database schema to live Supabase</name>
  <files></files>
  <read_first>
    supabase/migrations/00007_add_is_system_to_rules.sql,
    supabase/migrations/00008_seed_categories_and_rules.sql
  </read_first>
  <action>
Push the two new migrations to the live Supabase database:

```bash
supabase db push
```

If the command requires interactive confirmation (e.g., "Do you want to apply these migrations?"), try:
```bash
echo "y" | supabase db push
```

If the push requires a `SUPABASE_ACCESS_TOKEN` environment variable for non-TTY mode, the user will need to set it. Check if the token is available:
```bash
echo $SUPABASE_ACCESS_TOKEN
```

If not set, flag for manual intervention.

After push, verify the migrations were applied:
```bash
supabase db remote list
```

Expected: migrations 00007_add_is_system_to_rules and 00008_seed_categories_and_rules appear in the list.

Also verify TypeScript compilation passes:
```bash
npx tsc --noEmit
```

And verify all tests pass:
```bash
npx vitest run src/lib/rules/ --reporter=verbose 2>/dev/null || npx jest src/lib/rules/ --verbose 2>/dev/null
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - supabase db push completes successfully (exit code 0)
    - Migrations 00007 and 00008 appear in remote migration list
    - npx tsc --noEmit exits with code 0
    - Test files in src/lib/rules/ all pass
  </acceptance_criteria>
  <done>Database schema pushed to live Supabase. All migrations applied. TypeScript compiles. Tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full categorization engine end-to-end</name>
  <files></files>
  <action>
CHECKPOINT: Human verification required. Present the verification steps below to the user and wait for approval.

What was built:
Complete categorization engine: inline rule creation from review screen, real-time evaluation, 100% categorization gate, rules management page with CRUD/reorder/re-categorize, categories management page with inline editing, sidebar navigation enabled.

How to verify:
1. Start the dev server: `npm run dev`
2. Log in as admin

Test A -- Categories page:
3. Navigate to /categories via sidebar (should now be visible and clickable)
4. Verify 11 pre-seeded categories are shown (Card Payment with Lock icon at top, then 10 others alphabetically)
5. Try editing a user category name inline (click Pencil, change name, press Enter)
6. Try adding a new category (click "Add Category" at bottom, type name, press Enter)
7. Try deleting a user category with no rules (should show toast with Undo)
8. Verify Card Payment cannot be edited or deleted (Lock icon, disabled buttons)

Test B -- Rules page:
9. Navigate to /rules via sidebar
10. Verify system PAYMENT rule appears at top with Lock icon
11. Click "Add Rule" -- create a rule with pattern "GRAB", match type Substring, category Transport
12. Verify the new rule appears in the table below the system rule
13. Click Pencil on the new rule to edit inline, change pattern to "GRAB TRANSPORT", save
14. Test reorder: add another rule, use up/down arrows to change order
15. Delete the test rule -- verify toast with Undo appears. Click Undo to restore.

Test C -- Import flow with categorization:
16. Navigate to /import
17. Upload a Citibank SG PDF statement
18. Verify transactions appear in review screen with Categorized and Uncategorized sections
19. Verify Card Payment transactions (matching "PAYMENT") auto-appear in Categorized section with grey Card Payment badge and Lock icon
20. Verify import button is DISABLED with progress text (e.g., "5/23 categorized -- categorize all to import")
21. Click an uncategorized transaction -- verify popover opens with pre-filled pattern
22. Create a rule from the popover -- verify matching transactions instantly move to Categorized section
23. Repeat until 100% categorized
24. Verify import button ENABLES with "Import N Transactions" text
25. Click Import -- verify success toast
26. Navigate to /import/history -- verify the import appears with correct transaction count

Test D -- Re-categorize:
27. Go to /rules, edit an existing rule pattern
28. Click "Re-categorize All" -- verify toast shows "N updated, M unchanged"
29. Check /import/history or transaction data reflects the new categorization

Test E -- Mobile (resize browser to less than 768px):
30. Verify rules page shows cards instead of table
31. Verify categories page works with touch-friendly layout
32. Verify import review popover works on mobile (appears as bottom-aligned panel)

Resume signal: Type "approved" or describe issues found.
  </action>
  <verify>Human approval received</verify>
  <acceptance_criteria>
    - User confirms categories page shows 11 pre-seeded categories with Card Payment locked
    - User confirms rules page shows system PAYMENT rule, CRUD works, reorder works
    - User confirms import flow enforces 100% categorization gate
    - User confirms inline popover creates rules that immediately categorize matching transactions
    - User confirms re-categorize updates existing imported transactions
    - User confirms mobile layout works for rules and categories pages
  </acceptance_criteria>
  <done>Human verified full categorization engine end-to-end. All test scenarios passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema push -> Live database | Migrations modify production schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Tampering | Schema push | mitigate | Migrations are reviewed in Plan 01 Task 1 before push. Seed data uses known-good household UUID. DB triggers provide runtime protection for system rules. |
</threat_model>

<verification>
- Schema push succeeds
- TypeScript compiles
- Tests pass
- Full flow verified by human (upload -> categorize -> import)
</verification>

<success_criteria>
- Database schema live with is_system and seeded data
- Human verified: end-to-end categorization flow works
- Human verified: rules and categories pages functional
- Human verified: 100% gate enforced on import
</success_criteria>

<output>
After completion, create `.planning/phases/03-categorization-engine/03-04-SUMMARY.md`
</output>
