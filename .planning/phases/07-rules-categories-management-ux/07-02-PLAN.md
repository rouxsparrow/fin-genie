---
phase: 07-rules-categories-management-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/categories/categories-list.tsx
  - src/components/categories/category-item.tsx
autonomous: true
requirements:
  - CATM-01
  - CATM-02
  - CATM-03
  - CATM-04
must_haves:
  truths:
    - "Admin sees whether each category is Included in stats or Excluded from stats without relying on a tooltip."
    - "Admin sees system categories as protected."
    - "Admin can create and rename categories with visible empty, duplicate, and server-error feedback."
    - "Admin can use category edit, delete, and dashboard inclusion controls on smaller screens without hover."
  artifacts:
    - path: "src/components/categories/category-item.tsx"
      provides: "Visible dashboard inclusion status, protected system text, mobile-visible actions"
      contains: "Included in stats"
    - path: "src/components/categories/categories-list.tsx"
      provides: "Create/edit validation and server error propagation"
      contains: "categoryErrors"
  key_links:
    - from: "src/components/categories/categories-list.tsx"
      to: "src/components/categories/category-item.tsx"
      via: "`serverError` prop"
      pattern: "serverError"
    - from: "src/components/categories/category-item.tsx"
      to: "src/app/actions/category-actions.ts"
      via: "existing `onToggleExclude` callback"
      pattern: "onToggleExclude"
---

<objective>
Improve category-management scanability, validation feedback, and mobile action ergonomics.

Purpose: Satisfies D-10 through D-16 for categories while preserving existing category server actions and Neo Brutalism primitives.
Output: Updated category list and category item UI.
</objective>

<execution_context>
@/Users/rouxsparrow/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rouxsparrow/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-rules-categories-management-ux/07-CONTEXT.md
@.planning/phases/07-rules-categories-management-ux/07-RESEARCH.md
@src/app/(authenticated)/categories/page.tsx
@src/components/categories/categories-list.tsx
@src/components/categories/category-item.tsx
@src/app/actions/category-actions.ts

<interfaces>
From src/components/categories/category-item.tsx:
```ts
interface CategoryItemProps {
  category: Category;
  isEditing: boolean;
  onEdit: () => void;
  onCancelEdit: () => void;
  onSave: (name: string) => void;
  onDelete: () => void;
  onToggleExclude: (excludeFromStats: boolean) => void;
  isLast: boolean;
}
```

From src/app/actions/category-actions.ts:
```ts
export async function toggleCategoryExclude(input: {
  id: string;
  excludeFromStats: boolean;
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add visible category status and protected labels</name>
  <files>src/components/categories/category-item.tsx</files>
  <read_first>
    - src/components/categories/category-item.tsx
    - src/components/categories/categories-list.tsx
    - .planning/phases/07-rules-categories-management-ux/07-CONTEXT.md
  </read_first>
  <behavior>
    - D-10/D-11: category rows show `Included in stats` or `Excluded from stats` as always-visible text.
    - D-12: system categories show protected status text.
    - D-14/D-15/D-16: mobile controls remain visible without hover.
  </behavior>
  <action>
    In `CategoryItem`, compute `const statsStatus = category.exclude_from_stats ? 'Excluded from stats' : 'Included in stats';`. Render `statsStatus` as visible text for every non-system category next to the existing Eye/EyeOff toggle. Do not rely only on `TooltipContent`.

    For system categories, render visible text exactly `Protected system category` next to the lock icon, and render visible text exactly `Excluded from stats` because system categories are excluded by analytics behavior. Keep edit/delete/toggle disabled or unavailable for system categories.

    Replace the hover-only action visibility classes `opacity-0 transition-opacity group-hover:opacity-100 md:opacity-0 max-md:opacity-100` with always-visible action layout classes. Use a responsive structure that can wrap on mobile, for example a root container that allows `flex-col gap-2 py-3 sm:flex-row sm:items-center`, with category name/status in one group and actions in another group. Keep buttons using `variant="neutral"` and `className="h-8 w-8"` so the Neo Brutalism button style remains intact.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg "Included in stats" src/components/categories/category-item.tsx` returns a match.
    - `rg "Excluded from stats" src/components/categories/category-item.tsx` returns a match.
    - `rg "Protected system category" src/components/categories/category-item.tsx` returns a match.
    - `rg "group-hover:opacity-100|opacity-0" src/components/categories/category-item.tsx` returns no matches.
    - `rg "statsStatus" src/components/categories/category-item.tsx` returns a match.
    - `npx tsc --noEmit && npm run lint` exits 0.
  </acceptance_criteria>
  <done>Each category row/card exposes dashboard inclusion and protected-system status as text, and category controls are visible without hover.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface category create and edit errors inline</name>
  <files>src/components/categories/categories-list.tsx, src/components/categories/category-item.tsx</files>
  <read_first>
    - src/components/categories/categories-list.tsx
    - src/components/categories/category-item.tsx
    - src/app/actions/category-actions.ts
    - .planning/phases/07-rules-categories-management-ux/07-CONTEXT.md
  </read_first>
  <behavior>
    - D-13: empty and duplicate category names continue to be blocked.
    - CATM-02: server failures for create and rename are visible inline, not only as toasts.
  </behavior>
  <action>
    In `CategoriesList`, add `const [categoryErrors, setCategoryErrors] = useState<Record<string, string>>({});`. In `handleSaveEdit`, when the client duplicate check fails, set `categoryErrors[categoryId]` to exactly `A category with this name already exists.` and also keep the toast. When `updateCategory` returns `success: false`, set `categoryErrors[categoryId]` to `result.error` before calling `toast.error(result.error)`. When edit succeeds, remove that category ID from `categoryErrors`.

    In `handleAddCategory`, if `createCategory` returns `success: false`, set `newNameError` to `result.error` before calling `toast.error(result.error)`. Keep the existing exact client errors `Category name cannot be empty.` and `A category with this name already exists.`

    Extend `CategoryItemProps` with optional `serverError?: string`. Render `serverError` below the edit row using `text-[#ef4444]` and `text-xs font-medium`. Clear the category-specific server error when edit is cancelled and before retrying a save.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg "categoryErrors" src/components/categories/categories-list.tsx` returns a match.
    - `rg "serverError" src/components/categories/category-item.tsx src/components/categories/categories-list.tsx` returns matches in both files.
    - `rg "setNewNameError\\(result\\.error\\)" src/components/categories/categories-list.tsx` returns a match.
    - `rg "A category with this name already exists\\." src/components/categories/categories-list.tsx` returns a match.
    - `rg "Category name cannot be empty\\." src/components/categories/categories-list.tsx src/components/categories/category-item.tsx` returns matches.
    - `npx tsc --noEmit && npm run lint` exits 0.
  </acceptance_criteria>
  <done>Category create/rename failures are visible inline while existing empty and duplicate validation remains enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser -> categories client components | User-entered category names and dashboard inclusion toggles enter client validation before server action submission. |
| Categories client components -> server actions | Server actions remain the authority for admin authorization, system-category protection, and household-scoped writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Spoofing | `CategoryItem` status text | mitigate | Render literal `Included in stats`, `Excluded from stats`, and `Protected system category` labels so icon-only state cannot be misread. |
| T-07-06 | Tampering | Category rename/create validation | mitigate | Preserve empty and duplicate client checks and surface server errors inline; server actions still enforce Zod and admin checks. |
| T-07-07 | Elevation of Privilege | System category controls | mitigate | Do not render edit/delete/toggle controls for `category.is_system`; keep lock plus `Protected system category` text. |
| T-07-08 | Repudiation | Dashboard inclusion toggles | accept | Existing toast feedback remains; this plan does not add an audit log because category toggles are admin-only household maintenance. |
</threat_model>

<verification>
Run `npx tsc --noEmit && npm run lint`. Then verify the acceptance criteria with the listed `rg` commands.
</verification>

<success_criteria>
CATM-01 through CATM-04 are satisfied: category status is text-visible, protected categories are clear, validation failures are visible, and mobile users can access essential controls without hover.
</success_criteria>

<output>
After completion, create `.planning/phases/07-rules-categories-management-ux/07-rules-categories-management-ux-02-SUMMARY.md`
</output>
