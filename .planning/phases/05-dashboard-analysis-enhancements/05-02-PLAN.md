---
phase: "05"
plan: "02"
type: execute
wave: 2
depends_on:
  - 05-01-PLAN.md
files_modified:
  - src/components/dashboard/stat-card-grid.tsx
  - src/components/dashboard/monthly-bar-chart.tsx
  - src/components/dashboard/category-donut-chart.tsx
  - src/components/dashboard/chart-tooltip.tsx
  - src/components/dashboard/stat-card.tsx
  - src/app/(authenticated)/dashboard/page.tsx
autonomous: true
requirements:
  - ANLC-07
  - ANLC-09
  - ANLC-10
must_haves:
  truths:
    - "Dashboard cards and charts adapt to single-month, multi-month preset, and custom-range views."
    - "Single-month view shows the new comparison-focused total-spending card and the existing three drill-down cards."
    - "Single-month secondary chart becomes Category Trends."
    - "Multi-month preset view keeps the monthly spending chart and replaces the donut with a category-comparison bar chart."
    - "Custom-range view keeps the category chart and uses adaptive granularity for the time-series chart."
  artifacts:
    - path: "src/app/(authenticated)/dashboard/page.tsx"
      provides: "Range-aware dashboard composition"
    - path: "src/components/dashboard/stat-card-grid.tsx"
      provides: "Mode-aware summary card rendering"
    - path: "src/components/dashboard/monthly-bar-chart.tsx"
      provides: "Time-series chart behavior for multi-month and custom range modes"
    - path: "src/components/dashboard/category-donut-chart.tsx"
      provides: "Category chart rendering and selected-state behavior"
  key_links:
    - from: "src/app/(authenticated)/dashboard/page.tsx"
      to: "src/components/dashboard/stat-card-grid.tsx"
      via: "mode-aware summary data"
      pattern: "StatCardGrid"
    - from: "src/app/(authenticated)/dashboard/page.tsx"
      to: "chart components"
      via: "range-mode chart selection"
      pattern: "Category|Chart|Trend"
---

<objective>
Rebuild the visible dashboard summary layer for Phase 5: range-aware cards, range-aware chart
selection, and updated analytical presentation that matches the approved UI contract.

Purpose: the dashboard must feel substantially more analytical without breaking the app’s existing
visual language or overloading the user with unrelated new UI patterns.

Output: updated top cards and chart row across all dashboard range modes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-dashboard-analysis-enhancements/05-CONTEXT.md
@.planning/phases/05-dashboard-analysis-enhancements/05-RESEARCH.md
@.planning/phases/05-dashboard-analysis-enhancements/05-UI-SPEC.md
@src/app/(authenticated)/dashboard/page.tsx
@src/components/dashboard/stat-card-grid.tsx
@src/components/dashboard/stat-card.tsx
@src/components/dashboard/monthly-bar-chart.tsx
@src/components/dashboard/category-donut-chart.tsx
@src/components/dashboard/chart-tooltip.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make top summary cards mode-aware and drill-down ready</name>
  <read_first>
    - src/components/dashboard/stat-card-grid.tsx
    - src/components/dashboard/stat-card.tsx
    - src/app/(authenticated)/dashboard/page.tsx
    - .planning/phases/05-dashboard-analysis-enhancements/05-CONTEXT.md
    - .planning/phases/05-dashboard-analysis-enhancements/05-UI-SPEC.md
  </read_first>
  <files>src/components/dashboard/stat-card-grid.tsx, src/components/dashboard/stat-card.tsx, src/app/(authenticated)/dashboard/page.tsx</files>
  <action>
Refactor the dashboard summary cards so they render the correct content for each range mode.

Implement:
- single-month mode:
  - `Total Spending` with value, `vs last month`, and `vs {month} average`
  - existing `Top Category`, `Largest Transaction`, and `Recurring Spend` cards remain
- multi-month preset mode (`last-3-months`, `last-6-months`, `this-year`):
  - `Average Monthly Amount`
  - `Highest Month Amount`
  - `Lowest Month Amount`
  - top categories summary card showing the top 3 categories with amount and percentage
  - retain clickable largest-transaction and recurring-spend interactions where the layout requires them
- custom-range mode:
  - `Total Spending Amount` with `Avg / Day`
  - `Top Categories` with amount and percentage
  - `Largest Transaction`
  - `Total Days`

Keep typography, spacing, and emphasis aligned with `05-UI-SPEC.md`.
Make drill-down-capable cards expose click handlers without implementing modal contents in this plan.
  </action>
  <acceptance_criteria>
    - `src/components/dashboard/stat-card-grid.tsx` contains explicit branching for single-month, multi-month preset, and custom-range card sets
    - `src/components/dashboard/stat-card-grid.tsx` includes copy for `vs last month` and a month-average comparison in single-month mode
    - `src/components/dashboard/stat-card-grid.tsx` includes `Average Monthly Amount`, `Highest Month Amount`, and `Lowest Month Amount` for multi-month preset mode
    - `src/components/dashboard/stat-card-grid.tsx` includes `Avg / Day` and `Total Days` handling for custom range mode
  </acceptance_criteria>
  <verify>
    <automated>rg -n "single-month|multi-month|custom|vs last month|Average Monthly Amount|Lowest Month Amount|Avg / Day|Total Days" src/components/dashboard/stat-card-grid.tsx</automated>
  </verify>
  <done>The summary-card layer can render the locked single-month, multi-month, and custom metric sets without reusing one fixed card layout.</done>
</task>

<task type="auto">
  <name>Task 2: Rework the chart row for single-month, multi-month, and custom-range behavior</name>
  <read_first>
    - src/components/dashboard/monthly-bar-chart.tsx
    - src/components/dashboard/category-donut-chart.tsx
    - src/components/dashboard/chart-tooltip.tsx
    - src/app/(authenticated)/dashboard/page.tsx
    - .planning/phases/05-dashboard-analysis-enhancements/05-CONTEXT.md
    - .planning/phases/05-dashboard-analysis-enhancements/05-UI-SPEC.md
  </read_first>
  <files>src/components/dashboard/monthly-bar-chart.tsx, src/components/dashboard/category-donut-chart.tsx, src/components/dashboard/chart-tooltip.tsx, src/app/(authenticated)/dashboard/page.tsx</files>
  <action>
Update the dashboard chart row so each range mode renders the correct analysis surface:

- single-month mode:
  - replace the old monthly spending chart with `Category Trends`
  - render category-level up/down delta presentation versus previous month
- multi-month preset mode:
  - keep the monthly spending chart
  - replace the donut with a category-comparison bar chart that allows category comparison through the selected period
- custom-range mode:
  - keep the category breakdown chart
  - feed the time-series chart the adaptive daily/weekly/monthly series from plan 01 groundwork

Keep all chart surfaces within the existing Neo Brutalism card language and compact tooltip treatment.
Do not route users away from the dashboard for chart interactions in this plan.
  </action>
  <acceptance_criteria>
    - `src/app/(authenticated)/dashboard/page.tsx` branches chart selection by dashboard mode
    - single-month mode no longer uses the old monthly-spending chart as its secondary analysis chart
    - multi-month preset mode renders a category-comparison bar chart instead of the donut chart
    - custom-range mode preserves category breakdown and uses adaptive time-series data
    - updated chart components keep tooltip and label rendering within the styling rules from `05-UI-SPEC.md`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "single-month|multi-month|custom" 'src/app/(authenticated)/dashboard/page.tsx' && rg -n "Category Trends|category-comparison|adaptive|daily|weekly|monthly" src/components/dashboard/monthly-bar-chart.tsx src/components/dashboard/category-donut-chart.tsx src/components/dashboard/chart-tooltip.tsx 'src/app/(authenticated)/dashboard/page.tsx'</automated>
  </verify>
  <done>The dashboard chart row switches cleanly by range mode and reflects the locked Phase 5 analysis behavior instead of the old one-layout-fits-all design.</done>
</task>

<task type="auto">
  <name>Task 3: Verify visual-mode behavior and chart presentation contracts</name>
  <read_first>
    - src/app/(authenticated)/dashboard/page.tsx
    - src/components/dashboard/stat-card-grid.tsx
    - src/components/dashboard/monthly-bar-chart.tsx
    - src/components/dashboard/category-donut-chart.tsx
  </read_first>
  <files>src/app/(authenticated)/dashboard/page.tsx, src/components/dashboard/stat-card-grid.tsx, src/components/dashboard/monthly-bar-chart.tsx, src/components/dashboard/category-donut-chart.tsx</files>
  <action>
Run project verification for the updated Phase 5 dashboard summary layer.

At minimum:
- run TypeScript checking
- inspect the rendered-mode branching in dashboard/page and stat-card-grid
- verify that each range mode now has a distinct summary/charts contract instead of one reused layout
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits successfully
    - `src/app/(authenticated)/dashboard/page.tsx` contains distinct chart branching for single-month, multi-month preset, and custom-range modes
    - `src/components/dashboard/stat-card-grid.tsx` contains distinct card branching for all three dashboard modes
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The adaptive dashboard summary layer passes type-checking and has explicit card and chart branches for every Phase 5 range mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analytics payload -> card/chart rendering | Server-derived analytics are rendered into interactive UI states and labels |
| chart interactions -> dashboard filter state | User chart selections modify the same URL-backed state used by the rest of the dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | T | range-mode page branching | mitigate | Branch on one explicit dashboard mode contract rather than scattered ad-hoc checks across components |
| T-05-05 | I | category and summary charts | mitigate | Render only server-derived datasets that already respect excluded/system categories |
| T-05-06 | R | comparison labels and delta copy | mitigate | Keep comparison math centralized in analytics helpers so UI badges match the queried previous-equivalent period |
</threat_model>

<verification>
1. The dashboard renders different top-card content by range mode.
2. Single-month mode shows `Category Trends` instead of the old monthly-spending chart.
3. Multi-month presets keep the monthly trend and swap the donut for category comparison.
4. Custom-range mode preserves category breakdown and uses adaptive time-series input.
5. `npx tsc --noEmit` passes.
</verification>

<success_criteria>
- Range-aware dashboard visuals match the locked Phase 5 product behavior.
- Cards and charts adapt cleanly by dashboard mode.
- The UI remains consistent with the existing Neo Brutalism design system.
- The dashboard is ready for the final plan that embeds transactions and modal drill-downs.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-analysis-enhancements/05-dashboard-analysis-enhancements-02-SUMMARY.md`
</output>
