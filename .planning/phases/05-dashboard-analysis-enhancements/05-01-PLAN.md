---
phase: "05"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/hooks/use-date-range.ts
  - src/components/dashboard/date-range-selector.tsx
  - src/app/actions/analytics-actions.ts
  - src/app/(authenticated)/dashboard/page.tsx
autonomous: true
requirements:
  - ANLC-07
  - ANLC-10
must_haves:
  truths:
    - "Dashboard uses one canonical URL-backed query-state contract for date range and dashboard-linked filters."
    - "Single-month navigation replaces the old This Month preset with month stepping that cannot move into future months."
    - "Analytics helpers support equivalent-period comparisons and adaptive date bucketing needed by Phase 5 dashboard modes."
    - "Dashboard analysis mode is derived explicitly so later UI work can branch cleanly for single-month, multi-month preset, and custom-range views."
  artifacts:
    - path: "src/lib/hooks/use-date-range.ts"
      provides: "Canonical date-range and month-navigation state contract"
    - path: "src/components/dashboard/date-range-selector.tsx"
      provides: "Updated range controls with month navigator behavior"
    - path: "src/app/actions/analytics-actions.ts"
      provides: "Range-aware analytics helpers and comparison/bucketing functions"
    - path: "src/app/(authenticated)/dashboard/page.tsx"
      provides: "Dashboard mode derivation and shared query-state plumbing"
  key_links:
    - from: "src/lib/hooks/use-date-range.ts"
      to: "src/components/dashboard/date-range-selector.tsx"
      via: "month navigator and preset control contract"
      pattern: "preset|from|to"
    - from: "src/app/actions/analytics-actions.ts"
      to: "src/app/(authenticated)/dashboard/page.tsx"
      via: "range-aware dashboard data payloads"
      pattern: "fetchDashboard"
---

<objective>
Establish the Phase 5 foundation for dashboard state and analytics contracts. Replace the old
single preset-driven month behavior with a proper month navigator, introduce explicit dashboard
analysis modes, and add the comparison and bucketing helpers the later plans depend on.

Purpose: later UI work will be shallow and fragile unless the dashboard has one coherent state
model and one coherent set of analytics helpers for all range modes.

Output: updated date-range state, updated selector behavior, expanded analytics helpers, and
dashboard-level mode derivation ready for the UI and transactions plans.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-dashboard-analysis-enhancements/05-CONTEXT.md
@.planning/phases/05-dashboard-analysis-enhancements/05-RESEARCH.md
@.planning/phases/05-dashboard-analysis-enhancements/05-UI-SPEC.md
@src/lib/hooks/use-date-range.ts
@src/components/dashboard/date-range-selector.tsx
@src/app/actions/analytics-actions.ts
@src/app/(authenticated)/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor date-range state for month navigation and dashboard analysis modes</name>
  <read_first>
    - src/lib/hooks/use-date-range.ts
    - src/components/dashboard/date-range-selector.tsx
    - src/app/(authenticated)/dashboard/page.tsx
    - .planning/phases/05-dashboard-analysis-enhancements/05-CONTEXT.md
    - .planning/phases/05-dashboard-analysis-enhancements/05-UI-SPEC.md
  </read_first>
  <files>src/lib/hooks/use-date-range.ts, src/components/dashboard/date-range-selector.tsx, src/app/(authenticated)/dashboard/page.tsx</files>
  <action>
Refactor the date-range state contract so the dashboard can support both month navigation and
range-mode-specific rendering without introducing a second conflicting source of truth.

In `src/lib/hooks/use-date-range.ts`:
- keep `from`, `to`, and `preset` as the canonical URL-backed state
- preserve the existing preset names `this-month`, `last-3-months`, `last-6-months`, `this-year`, and `custom`
- add helper functions for month stepping in single-month mode:
  - previous month
  - next month
  - a guard that clamps next-month navigation at the current month
- add derived metadata helpers that expose whether the current range is:
  - `single-month`
  - `multi-month-preset`
  - `custom-range`
- expose the active viewed month label logic that matches the locked format:
  - current year -> month only, for example `April`
  - non-current year -> `MMM'YY`, for example `Dec'25`

In `src/components/dashboard/date-range-selector.tsx`:
- replace the old `This Month` button with a left-arrow / active month label / right-arrow control
- keep the other preset buttons and custom popover
- ensure the right arrow becomes visibly and semantically disabled at the current month
- keep styling aligned with the existing Neo Brutalism button language from `src/components/ui/button.tsx`

In `src/app/(authenticated)/dashboard/page.tsx`:
- derive one explicit dashboard analysis mode from the hook output and use it as the branching contract for later plans
- keep category filtering URL-backed and ready to synchronize with the embedded transactions section
- do not yet rebuild the full cards/charts section in this task; focus on introducing the state contract cleanly
  </action>
  <acceptance_criteria>
    - `src/lib/hooks/use-date-range.ts` contains helpers for month stepping and a derived dashboard analysis mode
    - `src/components/dashboard/date-range-selector.tsx` no longer renders the literal text `This Month`
    - `src/components/dashboard/date-range-selector.tsx` contains left/right month navigation controls
    - `src/components/dashboard/date-range-selector.tsx` includes logic that prevents moving past the current month
    - `src/app/(authenticated)/dashboard/page.tsx` contains explicit mode branching for single-month, multi-month preset, or custom-range analysis
  </acceptance_criteria>
  <verify>
    <automated>rg -n "single-month|multi-month|custom" src/lib/hooks/use-date-range.ts 'src/app/(authenticated)/dashboard/page.tsx' && ! rg -n "This Month" src/components/dashboard/date-range-selector.tsx && rg -n "ChevronLeft|ChevronRight|disabled" src/components/dashboard/date-range-selector.tsx</automated>
  </verify>
  <done>The dashboard has one URL-backed range contract that supports month navigation and explicit range-mode branching without introducing duplicate dashboard-only state.</done>
</task>

<task type="auto">
  <name>Task 2: Expand analytics actions for equivalent-period comparisons and adaptive bucketing</name>
  <read_first>
    - src/app/actions/analytics-actions.ts
    - src/lib/hooks/use-date-range.ts
    - .planning/phases/05-dashboard-analysis-enhancements/05-CONTEXT.md
    - .planning/phases/05-dashboard-analysis-enhancements/05-RESEARCH.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>src/app/actions/analytics-actions.ts</files>
  <action>
Extend `src/app/actions/analytics-actions.ts` so Phase 5 has the backend contracts required for
all three dashboard modes.

Add shared helpers for:
- equivalent-period comparison ranges for presets and custom ranges, not just previous calendar month
- same-calendar-month yearly average calculation for the single-month total-spending card
- adaptive chart bucketing:
  - 1-45 days -> daily
  - 46-120 days -> weekly
  - 121+ days -> monthly
- category delta calculation versus the previous month for single-month `Category Trends`
- top-N transaction/category summaries for later card and modal use

Preserve and reuse the current exclusion behavior for system and excluded categories in all new
analytics helpers.

Do not normalize merchants into schema in this phase. If merchant-style insights are needed for
summary calculations, derive them from the existing description field only.
  </action>
  <acceptance_criteria>
    - `src/app/actions/analytics-actions.ts` contains a reusable equivalent-period comparison helper beyond previous calendar month only
    - `src/app/actions/analytics-actions.ts` contains adaptive bucketing logic for daily, weekly, and monthly chart aggregation
    - `src/app/actions/analytics-actions.ts` contains category delta logic for previous-month comparison
    - `src/app/actions/analytics-actions.ts` continues to apply excluded/system category filtering in new analytics helpers
    - `src/app/actions/analytics-actions.ts` contains helper outputs that later plans can use for top-N category or transaction summaries
  </acceptance_criteria>
  <verify>
    <automated>rg -n "equivalent|comparison" src/app/actions/analytics-actions.ts && rg -n "45|120|daily|weekly|monthly" src/app/actions/analytics-actions.ts && rg -n "delta|top.?N|top category|largest" src/app/actions/analytics-actions.ts</automated>
  </verify>
  <done>The analytics layer exposes the comparison, bucketing, and summary helpers that later Phase 5 UI work can consume directly.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the Phase 5 foundation contract before UI implementation</name>
  <read_first>
    - src/lib/hooks/use-date-range.ts
    - src/components/dashboard/date-range-selector.tsx
    - src/app/actions/analytics-actions.ts
    - src/app/(authenticated)/dashboard/page.tsx
  </read_first>
  <files>src/lib/hooks/use-date-range.ts, src/components/dashboard/date-range-selector.tsx, src/app/actions/analytics-actions.ts, src/app/(authenticated)/dashboard/page.tsx</files>
  <action>
Run the project checks needed to confirm the new range-state and analytics foundation is stable
before the UI-heavy plans build on it.

At minimum:
- run TypeScript checking
- inspect the updated range selector and hook files to confirm the month navigator has replaced the old `This Month` control
- inspect the analytics action file to confirm the adaptive bucketing thresholds and equivalent-period comparison helpers exist
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits successfully
    - `src/components/dashboard/date-range-selector.tsx` contains no remaining `This Month` label
    - `src/app/actions/analytics-actions.ts` contains the exact thresholds `45`, `120`, and a monthly fallback for adaptive bucketing
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The Phase 5 foundation passes type-checking and the key month-navigation and bucketing contracts are present for downstream plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser query params -> dashboard state | Untrusted `from`, `to`, `preset`, and filter values seed the dashboard contract |
| dashboard state -> analytics actions | Client-selected range and filter state is translated into authenticated data queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | `use-date-range` and selector state | mitigate | Keep one typed URL-backed range contract and explicitly clamp next-month navigation at the current month |
| T-05-02 | I | `analytics-actions.ts` comparison helpers | mitigate | Reuse authenticated household-scoped queries and existing excluded-category filters in every new helper |
| T-05-03 | D | adaptive analytics queries | mitigate | Preserve bounded query patterns and compute comparisons/bucketing on scoped transaction sets rather than loading unbounded dashboard-wide histories |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes.
2. The month navigator replaces the old `This Month` button and cannot advance into the future.
3. The dashboard has an explicit internal mode contract for single-month, multi-month preset, and custom-range behavior.
4. Analytics helpers support equivalent-period comparison and adaptive time bucketing for later plans.
</verification>

<success_criteria>
- Phase 5 has one canonical query-state contract for dashboard range behavior.
- Month navigation is range-safe and URL-backed.
- Analytics helpers now support the comparison and bucketing logic required by Phase 5.
- Later UI plans can consume the new contracts without inventing duplicate state or math.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-analysis-enhancements/05-dashboard-analysis-enhancements-01-SUMMARY.md`
</output>
