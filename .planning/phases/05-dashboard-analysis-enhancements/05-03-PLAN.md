---
phase: "05"
plan: "03"
type: execute
wave: 3
depends_on:
  - 05-01-PLAN.md
  - 05-02-PLAN.md
files_modified:
  - src/components/dashboard/recent-transactions.tsx
  - src/app/(authenticated)/dashboard/page.tsx
  - src/components/transactions/transaction-search-bar.tsx
  - src/components/transactions/transaction-data-table.tsx
  - src/components/transactions/transaction-pagination.tsx
  - src/components/ui/dialog.tsx
  - src/app/actions/analytics-actions.ts
autonomous: true
requirements:
  - ANLC-08
  - ANLC-10
must_haves:
  truths:
    - "Transactions live directly on the dashboard page with search, sort, pagination, and current-filter context."
    - "The filter chip from donut/category selection lives in the transactions section, not at the top of the page."
    - "Top Category, Largest Transaction, and Recurring Spend open in-page modal overlays with the correct scoped transaction data."
    - "Dashboard interactions stay synchronized across cards, charts, modals, and the embedded transactions section."
  artifacts:
    - path: "src/components/dashboard/recent-transactions.tsx"
      provides: "Embedded dashboard transactions explorer replacing the preview-only recent-transactions card"
    - path: "src/app/(authenticated)/dashboard/page.tsx"
      provides: "Synchronized state wiring for transactions and modal drill-downs"
    - path: "src/app/actions/analytics-actions.ts"
      provides: "Scoped transaction queries for modals and the dashboard transactions explorer"
    - path: "src/components/ui/dialog.tsx"
      provides: "Modal overlay behavior used for card drill-downs"
  key_links:
    - from: "src/app/(authenticated)/dashboard/page.tsx"
      to: "src/components/dashboard/recent-transactions.tsx"
      via: "shared dashboard filter, search, sort, and pagination state"
      pattern: "Transactions"
    - from: "summary cards"
      to: "modal overlay data fetches"
      via: "top category / largest transaction / subscriptions drill-downs"
      pattern: "Dialog|modal|open"
---

<objective>
Finish Phase 5 by turning the dashboard into the actual analysis workspace: embed the full
transactions explorer on the dashboard and implement the modal drill-down overlays from the
summary cards.

Purpose: this is the plan that delivers the end-user payoff of the new dashboard design by making
analysis and drill-down happen in one place instead of through preview-only widgets.

Output: embedded dashboard transactions explorer, moved filter context, and working in-page modal drill-downs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-dashboard-analysis-enhancements/05-CONTEXT.md
@.planning/phases/05-dashboard-analysis-enhancements/05-RESEARCH.md
@.planning/phases/05-dashboard-analysis-enhancements/05-UI-SPEC.md
@src/app/(authenticated)/dashboard/page.tsx
@src/components/dashboard/recent-transactions.tsx
@src/components/transactions/transaction-search-bar.tsx
@src/components/transactions/transaction-data-table.tsx
@src/components/transactions/transaction-pagination.tsx
@src/components/ui/dialog.tsx
@src/app/actions/analytics-actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace preview-only recent transactions with an embedded dashboard transactions explorer</name>
  <read_first>
    - src/components/dashboard/recent-transactions.tsx
    - src/app/(authenticated)/dashboard/page.tsx
    - src/components/transactions/transaction-search-bar.tsx
    - src/components/transactions/transaction-data-table.tsx
    - src/components/transactions/transaction-pagination.tsx
    - .planning/phases/05-dashboard-analysis-enhancements/05-CONTEXT.md
    - .planning/phases/05-dashboard-analysis-enhancements/05-UI-SPEC.md
  </read_first>
  <files>src/components/dashboard/recent-transactions.tsx, src/app/(authenticated)/dashboard/page.tsx, src/components/transactions/transaction-search-bar.tsx, src/components/transactions/transaction-data-table.tsx, src/components/transactions/transaction-pagination.tsx</files>
  <action>
Turn the current preview-only `RecentTransactions` experience into the full embedded dashboard
transactions section required by Phase 5.

Implement all of the following:
- rename the section to `Transactions`
- remove the `View All` button entirely
- render the full selected-period transaction set, not only 10 preview rows
- add search inside the dashboard transactions block
- add sortable `Date` and `Amount`
- add pagination at 10 results per page
- move the active donut/category filter chip into the transactions section, near the controls

Reuse transaction-table primitives from the transactions page where it helps, but keep the
dashboard transactions section governed by dashboard state instead of copying the full page logic wholesale.
  </action>
  <acceptance_criteria>
    - `src/components/dashboard/recent-transactions.tsx` contains the title `Transactions`
    - `src/components/dashboard/recent-transactions.tsx` no longer contains `View All`
    - the dashboard transactions section includes search UI
    - the dashboard transactions section includes sort handling for `Date` and `Amount`
    - the dashboard transactions section includes pagination at 10 results per page
    - the active category filter chip is rendered inside the transactions section rather than near the top of `dashboard/page.tsx`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Transactions" src/components/dashboard/recent-transactions.tsx && ! rg -n "View All" src/components/dashboard/recent-transactions.tsx && rg -n "search|sort|pagination|pageSize|10 results|Date|Amount" src/components/dashboard/recent-transactions.tsx 'src/app/(authenticated)/dashboard/page.tsx'</automated>
  </verify>
  <done>The dashboard owns a full embedded transactions explorer with search, sorting, filter context, and 10-row pagination instead of a preview-only card.</done>
</task>

<task type="auto">
  <name>Task 2: Implement in-page modal drill-downs for top category, largest transactions, and subscriptions</name>
  <read_first>
    - src/app/(authenticated)/dashboard/page.tsx
    - src/components/dashboard/stat-card-grid.tsx
    - src/components/ui/dialog.tsx
    - src/app/actions/analytics-actions.ts
    - .planning/phases/05-dashboard-analysis-enhancements/05-CONTEXT.md
    - .planning/phases/05-dashboard-analysis-enhancements/05-UI-SPEC.md
  </read_first>
  <files>src/app/(authenticated)/dashboard/page.tsx, src/components/ui/dialog.tsx, src/app/actions/analytics-actions.ts</files>
  <action>
Implement the in-page modal drill-down overlays required by the locked Phase 5 decisions.

Add dashboard-local modal flows for:
- `Top Category` -> all transactions in the selected top category for the selected month, sorted by amount descending
- `Largest Transaction` -> 5 largest transactions for the active month or period, sorted by amount descending
- `Recurring Spend` -> all transactions in the `Subscriptions` category for the selected month, sorted by amount descending

Use the existing dialog primitive in `src/components/ui/dialog.tsx` and style the modal surfaces
to match the Neo Brutalism white-card contract from `05-UI-SPEC.md`.
Do not route to `/transactions`.
Keep focus management and close behavior accessible.
  </action>
  <acceptance_criteria>
    - `src/app/(authenticated)/dashboard/page.tsx` contains modal open/close state for card drill-downs
    - modal drill-downs use the dialog primitive instead of route navigation
    - analytics actions expose scoped queries for top category, largest transactions, and subscriptions drill-down data
    - largest-transactions drill-down limits results to 5 items
    - modal transaction lists are ordered by amount descending
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Dialog|open|onOpenChange" 'src/app/(authenticated)/dashboard/page.tsx' src/components/ui/dialog.tsx && rg -n "top category|largest|subscriptions|amount descending|limit.?5" src/app/actions/analytics-actions.ts 'src/app/(authenticated)/dashboard/page.tsx'</automated>
  </verify>
  <done>The dashboard’s insight cards open accessible in-page drill-down overlays seeded from the active dashboard context instead of navigating to another route.</done>
</task>

<task type="auto">
  <name>Task 3: Verify synchronization between dashboard filters, transactions, and drill-down overlays</name>
  <read_first>
    - src/app/(authenticated)/dashboard/page.tsx
    - src/components/dashboard/recent-transactions.tsx
    - src/app/actions/analytics-actions.ts
  </read_first>
  <files>src/app/(authenticated)/dashboard/page.tsx, src/components/dashboard/recent-transactions.tsx, src/app/actions/analytics-actions.ts</files>
  <action>
Run final Phase 5 verification checks focused on synchronized behavior rather than isolated rendering.

At minimum:
- run TypeScript checking
- inspect dashboard state wiring to confirm cards, charts, transactions, and modals all derive from the same range/filter contract
- verify the embedded transactions section and drill-down overlays are not using stale preview-only assumptions
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits successfully
    - `src/app/(authenticated)/dashboard/page.tsx` shares dashboard state across chart filtering, transactions, and modal drill-downs
    - `src/components/dashboard/recent-transactions.tsx` no longer behaves as a preview-only component
    - modal drill-downs use active dashboard range/filter context instead of unrelated hard-coded query inputs
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The final Phase 5 dashboard interactions stay synchronized across charts, cards, modal overlays, and the embedded transactions explorer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard controls -> transaction queries | Search, sort, page, and category inputs seed transaction fetching on the dashboard |
| card clicks -> modal data loaders | Drill-down interactions request scoped transaction subsets for overlays |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | T | embedded transaction explorer | mitigate | Reuse validated dashboard query state and bounded 10-row pagination rather than ad-hoc client filtering |
| T-05-08 | I | modal drill-down queries | mitigate | Keep all modal data loads household-scoped through authenticated analytics actions with explicit category/range inputs |
| T-05-09 | D | dashboard search and pagination | mitigate | Preserve debounced search and server-side pagination so the embedded explorer does not fetch the entire period into the client |
</threat_model>

<verification>
1. The dashboard contains a full embedded `Transactions` section with search, sort, and 10-per-page pagination.
2. Category filtering is presented inside the transactions section.
3. `Top Category`, `Largest Transaction`, and `Recurring Spend` open in-page modal overlays instead of routing away.
4. Modal lists show the correct scoped transaction data and amount-desc ordering.
5. `npx tsc --noEmit` passes.
</verification>

<success_criteria>
- Dashboard analysis and drill-down now happen in one place.
- Embedded transactions and modal drill-downs are synchronized with dashboard range and filter state.
- The preview-style Phase 4 transactions block is fully replaced by the richer Phase 5 analysis surface.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-analysis-enhancements/05-dashboard-analysis-enhancements-03-SUMMARY.md`
</output>
