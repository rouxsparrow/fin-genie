---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - postcss.config.mjs
  - src/app/globals.css
  - src/app/layout.tsx
  - src/lib/utils.ts
  - components.json
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/input.tsx
  - src/components/ui/label.tsx
  - src/components/ui/select.tsx
  - src/components/ui/table.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/sonner.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/separator.tsx
  - supabase/config.toml
  - supabase/migrations/00001_initial_schema.sql
  - src/lib/supabase/client.ts
  - src/lib/supabase/server.ts
  - src/lib/supabase/middleware.ts
  - src/middleware.ts
  - src/lib/types/database.ts
  - .env.local.example
autonomous: true
requirements:
  - INFR-01
  - INFR-02
  - INFR-03
  - AUTH-01
  - AUTH-02
  - AUTH-04

must_haves:
  truths:
    - "Next.js dev server starts and renders without errors"
    - "Neo Brutalism amber theme is applied globally with correct CSS variables"
    - "DM Sans font loads via next/font/google"
    - "Supabase migration creates profiles table with role column and household_id"
    - "RLS policies enforce admin/viewer permissions on profiles table"
    - "Auth middleware refreshes sessions and redirects unauthenticated users"
    - "All monetary-ready columns use INTEGER type for cents storage"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Neo Brutalism amber theme CSS variables"
      contains: "--main"
    - path: "src/app/layout.tsx"
      provides: "Root layout with DM Sans font and Sonner toast provider"
      contains: "DM_Sans"
    - path: "supabase/migrations/00001_initial_schema.sql"
      provides: "Database schema with profiles, RLS policies"
      contains: "CREATE TABLE"
    - path: "src/lib/supabase/client.ts"
      provides: "Browser Supabase client"
      exports: ["createClient"]
    - path: "src/lib/supabase/server.ts"
      provides: "Server Supabase client"
      exports: ["createClient"]
    - path: "src/middleware.ts"
      provides: "Auth middleware for session refresh"
      contains: "updateSession"
  key_links:
    - from: "src/middleware.ts"
      to: "src/lib/supabase/middleware.ts"
      via: "import updateSession"
      pattern: "import.*updateSession.*middleware"
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: "import.*globals.css"
    - from: "supabase/migrations/00001_initial_schema.sql"
      to: "auth.users"
      via: "foreign key reference"
      pattern: "REFERENCES auth.users"
---

<objective>
Scaffold the Next.js project with Neo Brutalism amber theme, install all shadcn/neobrutalism UI components, create the Supabase database schema with RLS policies, and wire up auth utilities (browser client, server client, middleware).

Purpose: Establishes the complete foundation that all subsequent plans build on -- theme, components, database, and auth plumbing.
Output: Running Next.js app with Neo Brutalism styling, all UI components installed, Supabase schema with profiles table and RLS, and auth middleware.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project, install dependencies, configure Neo Brutalism theme and all UI components</name>
  <files>
    package.json, tsconfig.json, next.config.ts, tailwind.config.ts, postcss.config.mjs,
    src/app/globals.css, src/app/layout.tsx, src/lib/utils.ts, components.json,
    src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/input.tsx,
    src/components/ui/label.tsx, src/components/ui/select.tsx, src/components/ui/table.tsx,
    src/components/ui/dialog.tsx, src/components/ui/badge.tsx, src/components/ui/tooltip.tsx,
    src/components/ui/skeleton.tsx, src/components/ui/sonner.tsx, src/components/ui/sheet.tsx,
    src/components/ui/separator.tsx, .env.local.example
  </files>
  <read_first>
    .planning/phases/01-foundation-auth/01-UI-SPEC.md (design system, color tokens, typography, component inventory)
    .planning/phases/01-foundation-auth/01-CONTEXT.md (decisions D-17 through D-21, D-31)
    CLAUDE.md (technology stack, installation instructions, neo brutalism theme setup)
  </read_first>
  <action>
    **Step 1: Create Next.js project**
    Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --no-import-alias --turbopack` in the project root.
    This creates the project scaffold with TypeScript, Tailwind CSS 4, ESLint, App Router, and src/ directory.

    **Step 2: Install dependencies**
    Run:
    ```
    npm install @supabase/supabase-js @supabase/ssr zod date-fns nuqs sonner lucide-react
    npm install -D prettier prettier-plugin-tailwindcss
    ```

    **Step 3: Initialize shadcn and install Neo Brutalism components**
    Run `npx shadcn@latest init` -- select "New York" style, CSS variables mode.

    Then install all neobrutalism components one by one:
    ```
    npx shadcn@latest add @neobrutalism/button
    npx shadcn@latest add @neobrutalism/card
    npx shadcn@latest add @neobrutalism/input
    npx shadcn@latest add @neobrutalism/label
    npx shadcn@latest add @neobrutalism/select
    npx shadcn@latest add @neobrutalism/table
    npx shadcn@latest add @neobrutalism/dialog
    npx shadcn@latest add @neobrutalism/badge
    npx shadcn@latest add @neobrutalism/tooltip
    npx shadcn@latest add @neobrutalism/skeleton
    npx shadcn@latest add @neobrutalism/sonner
    npx shadcn@latest add @neobrutalism/sheet
    npx shadcn@latest add separator
    ```

    **Step 4: Configure Neo Brutalism amber theme in globals.css (per D-17, D-18, D-20)**
    Replace the generated globals.css with Neo Brutalism amber theme CSS variables from the neobrutalism.dev registry. The key CSS custom properties must include:
    - `--background: oklch(96.22% 0.0569 95.61)` (warm cream)
    - `--secondary-background: oklch(100% 0 0)` (white)
    - `--main: oklch(84.08% 0.1725 84.2)` (amber accent)
    - `--foreground: oklch(0% 0 0)` (black)
    - `--border: oklch(0% 0 0)` (black)
    - `--overlay: oklch(0% 0 0 / 0.8)` (overlay)
    - `--box-shadow-x: 4px`
    - `--box-shadow-y: 4px`
    - `--border-radius: 5px`
    - `--heading-font-weight: 700`
    - `--base-font-weight: 500`
    - Chart colors: `--chart-1: #FFBF00`, `--chart-2: #0099FF`, `--chart-3: #FF7A05`, `--chart-4: #00D696`, `--chart-5: #7A83FF`

    The shadow utility class `shadow-shadow` should resolve to `4px 4px 0px 0px var(--border)`.
    Only include `:root` (light mode) -- no `.dark` section (D-20).

    Add `@media (prefers-reduced-motion: reduce)` rule that sets `--box-shadow-x: 0px; --box-shadow-y: 0px` and disables transitions.

    **Step 5: Configure root layout with DM Sans font (per D-21, UI-SPEC Typography)**
    In `src/app/layout.tsx`:
    - Import `DM_Sans` from `next/font/google` with `subsets: ['latin']`, `display: 'swap'`, `adjustFontFallback: false`, `weight: ['500', '700']`
    - Apply the font variable to the html element via `className={dmSans.className}`
    - Import `globals.css`
    - Add `<Toaster />` from `sonner` component with `theme="light"` and `position="bottom-right"` and `toastOptions={{ duration: 4000 }}` (per D-31, UI-SPEC Toast section)
    - Set metadata: `title: "Fin Genie"`, `description: "Household finance analyzer"`
    - Wrap children in a minimal structure -- no header, no sidebar (those come in Plan 02)

    **Step 6: Create .env.local.example**
    Create file with:
    ```
    NEXT_PUBLIC_SUPABASE_URL=your-project-url
    NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
    SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
    ```

    **Step 7: Verify sonner component handles light-mode only**
    Per UI-SPEC Registry Safety note: `@neobrutalism/sonner` imports `useTheme` from `next-themes`. Since this project is light-mode only (D-20), modify the generated `src/components/ui/sonner.tsx` to either:
    - Remove the `next-themes` import and `useTheme` hook
    - Hardcode `theme="light"` passed to the Sonner component
    This avoids adding `next-themes` as a dependency.
  </action>
  <verify>
    <automated>cd /Users/rouxsparrow/Code/fin-genie && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"next"`, `"@supabase/supabase-js"`, `"@supabase/ssr"`, `"sonner"`, `"lucide-react"`, `"zod"`, `"date-fns"`, `"nuqs"`
    - src/app/globals.css contains `--main: oklch(84.08%` (amber accent)
    - src/app/globals.css contains `--background: oklch(96.22%` (cream)
    - src/app/globals.css contains `--box-shadow-x: 4px`
    - src/app/globals.css does NOT contain `.dark` selector
    - src/app/layout.tsx contains `DM_Sans` import from `next/font/google`
    - src/app/layout.tsx contains `Toaster` with `theme="light"`
    - src/components/ui/button.tsx exists (neobrutalism variant)
    - src/components/ui/card.tsx exists
    - src/components/ui/sheet.tsx exists
    - src/components/ui/sonner.tsx does NOT contain `useTheme` from `next-themes` OR hardcodes `theme="light"`
    - .env.local.example contains `NEXT_PUBLIC_SUPABASE_URL`
    - components.json exists
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>
    Next.js project scaffolded with all dependencies installed, shadcn initialized with all 12 neobrutalism components + separator, Neo Brutalism amber theme applied in globals.css, DM Sans font configured, Sonner toast provider in root layout, and build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Supabase database schema with RLS policies and auth utilities</name>
  <files>
    supabase/config.toml, supabase/migrations/00001_initial_schema.sql,
    src/lib/supabase/client.ts, src/lib/supabase/server.ts,
    src/lib/supabase/middleware.ts, src/middleware.ts,
    src/lib/types/database.ts
  </files>
  <read_first>
    .planning/phases/01-foundation-auth/01-CONTEXT.md (decisions D-24 through D-29)
    .planning/phases/01-foundation-auth/01-UI-SPEC.md (not needed but available)
    CLAUDE.md (Supabase patterns, RLS strategy, migration workflow, money handling strategy)
  </read_first>
  <action>
    **Step 1: Initialize Supabase project structure**
    Run `npx supabase init` in the project root. This creates `supabase/config.toml` and `supabase/` directory structure.

    **Step 2: Create initial migration file `supabase/migrations/00001_initial_schema.sql`**

    The migration must create:

    ```sql
    -- Enable UUID extension
    CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

    -- Profiles table (per D-24, D-25, D-26, D-28, D-29)
    CREATE TABLE profiles (
      id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
      household_id UUID NOT NULL DEFAULT '00000000-0000-0000-0000-000000000000'::uuid,
      full_name TEXT NOT NULL,
      email TEXT NOT NULL,
      role TEXT NOT NULL DEFAULT 'viewer' CHECK (role IN ('admin', 'viewer')),
      created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
      updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    );

    -- Index on household_id for future multi-household queries
    CREATE INDEX idx_profiles_household_id ON profiles(household_id);

    -- Updated_at trigger function
    CREATE OR REPLACE FUNCTION update_updated_at_column()
    RETURNS TRIGGER AS $$
    BEGIN
      NEW.updated_at = NOW();
      RETURN NEW;
    END;
    $$ language 'plpgsql';

    CREATE TRIGGER update_profiles_updated_at
      BEFORE UPDATE ON profiles
      FOR EACH ROW
      EXECUTE FUNCTION update_updated_at_column();

    -- Enable RLS (per INFR-02)
    ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;

    -- RLS Policies for profiles table
    -- All authenticated users in the same household can read all profiles
    CREATE POLICY "Users can view household profiles"
      ON profiles FOR SELECT
      TO authenticated
      USING (
        household_id = (
          SELECT p.household_id FROM profiles p WHERE p.id = auth.uid()
        )
      );

    -- Only admins can insert new profiles
    CREATE POLICY "Admins can create profiles"
      ON profiles FOR INSERT
      TO authenticated
      WITH CHECK (
        EXISTS (
          SELECT 1 FROM profiles p
          WHERE p.id = auth.uid() AND p.role = 'admin'
        )
      );

    -- Only admins can update profiles (role changes)
    CREATE POLICY "Admins can update profiles"
      ON profiles FOR UPDATE
      TO authenticated
      USING (
        EXISTS (
          SELECT 1 FROM profiles p
          WHERE p.id = auth.uid() AND p.role = 'admin'
        )
      );

    -- Only admins can delete profiles
    CREATE POLICY "Admins can delete profiles"
      ON profiles FOR DELETE
      TO authenticated
      USING (
        EXISTS (
          SELECT 1 FROM profiles p
          WHERE p.id = auth.uid() AND p.role = 'admin'
        )
      );

    -- Categories table (per D-26, D-25, D-29, INFR-01)
    CREATE TABLE categories (
      id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
      household_id UUID NOT NULL DEFAULT '00000000-0000-0000-0000-000000000000'::uuid,
      name TEXT NOT NULL,
      is_system BOOLEAN NOT NULL DEFAULT FALSE,
      created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
      updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    );

    CREATE INDEX idx_categories_household_id ON categories(household_id);

    CREATE TRIGGER update_categories_updated_at
      BEFORE UPDATE ON categories
      FOR EACH ROW
      EXECUTE FUNCTION update_updated_at_column();

    ALTER TABLE categories ENABLE ROW LEVEL SECURITY;

    CREATE POLICY "Users can view household categories"
      ON categories FOR SELECT
      TO authenticated
      USING (
        household_id = (
          SELECT p.household_id FROM profiles p WHERE p.id = auth.uid()
        )
      );

    CREATE POLICY "Admins can manage categories"
      ON categories FOR ALL
      TO authenticated
      USING (
        EXISTS (
          SELECT 1 FROM profiles p
          WHERE p.id = auth.uid() AND p.role = 'admin'
        )
      );

    -- Rules table (per D-26, D-25, D-29)
    CREATE TABLE rules (
      id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
      household_id UUID NOT NULL DEFAULT '00000000-0000-0000-0000-000000000000'::uuid,
      category_id UUID NOT NULL REFERENCES categories(id) ON DELETE CASCADE,
      pattern TEXT NOT NULL,
      match_type TEXT NOT NULL DEFAULT 'substring' CHECK (match_type IN ('substring', 'regex')),
      sort_order INTEGER NOT NULL DEFAULT 0,
      created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
      updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    );

    CREATE INDEX idx_rules_household_id ON rules(household_id);
    CREATE INDEX idx_rules_sort_order ON rules(sort_order);

    CREATE TRIGGER update_rules_updated_at
      BEFORE UPDATE ON rules
      FOR EACH ROW
      EXECUTE FUNCTION update_updated_at_column();

    ALTER TABLE rules ENABLE ROW LEVEL SECURITY;

    CREATE POLICY "Users can view household rules"
      ON rules FOR SELECT
      TO authenticated
      USING (
        household_id = (
          SELECT p.household_id FROM profiles p WHERE p.id = auth.uid()
        )
      );

    CREATE POLICY "Admins can manage rules"
      ON rules FOR ALL
      TO authenticated
      USING (
        EXISTS (
          SELECT 1 FROM profiles p
          WHERE p.id = auth.uid() AND p.role = 'admin'
        )
      );

    -- Imports table (per D-26, D-25, D-29)
    CREATE TABLE imports (
      id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
      household_id UUID NOT NULL DEFAULT '00000000-0000-0000-0000-000000000000'::uuid,
      file_name TEXT NOT NULL,
      statement_period_start DATE,
      statement_period_end DATE,
      transaction_count INTEGER NOT NULL DEFAULT 0,
      imported_by UUID NOT NULL REFERENCES auth.users(id),
      created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    );

    CREATE INDEX idx_imports_household_id ON imports(household_id);

    ALTER TABLE imports ENABLE ROW LEVEL SECURITY;

    CREATE POLICY "Users can view household imports"
      ON imports FOR SELECT
      TO authenticated
      USING (
        household_id = (
          SELECT p.household_id FROM profiles p WHERE p.id = auth.uid()
        )
      );

    CREATE POLICY "Admins can create imports"
      ON imports FOR INSERT
      TO authenticated
      WITH CHECK (
        EXISTS (
          SELECT 1 FROM profiles p
          WHERE p.id = auth.uid() AND p.role = 'admin'
        )
      );

    -- Transactions table (per D-26, D-25, D-29, INFR-01)
    CREATE TABLE transactions (
      id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
      household_id UUID NOT NULL DEFAULT '00000000-0000-0000-0000-000000000000'::uuid,
      import_id UUID NOT NULL REFERENCES imports(id) ON DELETE CASCADE,
      category_id UUID REFERENCES categories(id) ON DELETE SET NULL,
      transaction_date DATE NOT NULL,
      description TEXT NOT NULL,
      amount_cents INTEGER NOT NULL,
      is_debit BOOLEAN NOT NULL DEFAULT TRUE,
      transaction_hash TEXT NOT NULL,
      created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
      updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    );

    CREATE UNIQUE INDEX idx_transactions_hash ON transactions(transaction_hash);
    CREATE INDEX idx_transactions_household_id ON transactions(household_id);
    CREATE INDEX idx_transactions_date ON transactions(transaction_date);
    CREATE INDEX idx_transactions_category_id ON transactions(category_id);

    CREATE TRIGGER update_transactions_updated_at
      BEFORE UPDATE ON transactions
      FOR EACH ROW
      EXECUTE FUNCTION update_updated_at_column();

    ALTER TABLE transactions ENABLE ROW LEVEL SECURITY;

    CREATE POLICY "Users can view household transactions"
      ON transactions FOR SELECT
      TO authenticated
      USING (
        household_id = (
          SELECT p.household_id FROM profiles p WHERE p.id = auth.uid()
        )
      );

    CREATE POLICY "Admins can manage transactions"
      ON transactions FOR ALL
      TO authenticated
      USING (
        EXISTS (
          SELECT 1 FROM profiles p
          WHERE p.id = auth.uid() AND p.role = 'admin'
        )
      );
    ```

    **Step 3: Create TypeScript database types `src/lib/types/database.ts`**

    Define TypeScript types that mirror the database schema:
    ```typescript
    export type UserRole = 'admin' | 'viewer';
    export type MatchType = 'substring' | 'regex';

    export interface Profile {
      id: string;
      household_id: string;
      full_name: string;
      email: string;
      role: UserRole;
      created_at: string;
      updated_at: string;
    }

    export interface Category {
      id: string;
      household_id: string;
      name: string;
      is_system: boolean;
      created_at: string;
      updated_at: string;
    }

    export interface Rule {
      id: string;
      household_id: string;
      category_id: string;
      pattern: string;
      match_type: MatchType;
      sort_order: number;
      created_at: string;
      updated_at: string;
    }

    export interface Import {
      id: string;
      household_id: string;
      file_name: string;
      statement_period_start: string | null;
      statement_period_end: string | null;
      transaction_count: number;
      imported_by: string;
      created_at: string;
    }

    export interface Transaction {
      id: string;
      household_id: string;
      import_id: string;
      category_id: string | null;
      transaction_date: string;
      description: string;
      amount_cents: number;
      is_debit: boolean;
      transaction_hash: string;
      created_at: string;
      updated_at: string;
    }

    // Supabase Database type helper
    export interface Database {
      public: {
        Tables: {
          profiles: { Row: Profile; Insert: Omit<Profile, 'created_at' | 'updated_at'>; Update: Partial<Omit<Profile, 'id' | 'created_at' | 'updated_at'>>; };
          categories: { Row: Category; Insert: Omit<Category, 'id' | 'created_at' | 'updated_at'>; Update: Partial<Omit<Category, 'id' | 'created_at' | 'updated_at'>>; };
          rules: { Row: Rule; Insert: Omit<Rule, 'id' | 'created_at' | 'updated_at'>; Update: Partial<Omit<Rule, 'id' | 'created_at' | 'updated_at'>>; };
          imports: { Row: Import; Insert: Omit<Import, 'id' | 'created_at'>; Update: Partial<Omit<Import, 'id' | 'created_at'>>; };
          transactions: { Row: Transaction; Insert: Omit<Transaction, 'id' | 'created_at' | 'updated_at'>; Update: Partial<Omit<Transaction, 'id' | 'created_at' | 'updated_at'>>; };
        };
      };
    }
    ```

    **Step 4: Create Supabase browser client `src/lib/supabase/client.ts`**
    ```typescript
    import { createBrowserClient } from '@supabase/ssr';
    import { Database } from '@/lib/types/database';

    export function createClient() {
      return createBrowserClient<Database>(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
      );
    }
    ```

    **Step 5: Create Supabase server client `src/lib/supabase/server.ts`**
    ```typescript
    import { createServerClient } from '@supabase/ssr';
    import { cookies } from 'next/headers';
    import { Database } from '@/lib/types/database';

    export async function createClient() {
      const cookieStore = await cookies();
      return createServerClient<Database>(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
        {
          cookies: {
            getAll() {
              return cookieStore.getAll();
            },
            setAll(cookiesToSet) {
              try {
                cookiesToSet.forEach(({ name, value, options }) =>
                  cookieStore.set(name, value, options)
                );
              } catch {
                // Called from Server Component -- setAll can be ignored
              }
            },
          },
        }
      );
    }
    ```

    **Step 6: Create auth middleware helper `src/lib/supabase/middleware.ts`**
    ```typescript
    import { createServerClient } from '@supabase/ssr';
    import { NextResponse, type NextRequest } from 'next/server';

    export async function updateSession(request: NextRequest) {
      let supabaseResponse = NextResponse.next({ request });
      const supabase = createServerClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
        {
          cookies: {
            getAll() {
              return request.cookies.getAll();
            },
            setAll(cookiesToSet) {
              cookiesToSet.forEach(({ name, value }) =>
                request.cookies.set(name, value)
              );
              supabaseResponse = NextResponse.next({ request });
              cookiesToSet.forEach(({ name, value, options }) =>
                supabaseResponse.cookies.set(name, value, options)
              );
            },
          },
        }
      );

      const { data: { user } } = await supabase.auth.getUser();

      // Redirect unauthenticated users to /login (except login and auth callback routes)
      if (
        !user &&
        !request.nextUrl.pathname.startsWith('/login') &&
        !request.nextUrl.pathname.startsWith('/auth')
      ) {
        const url = request.nextUrl.clone();
        url.pathname = '/login';
        return NextResponse.redirect(url);
      }

      // Redirect authenticated users away from /login to /dashboard
      if (user && request.nextUrl.pathname.startsWith('/login')) {
        const url = request.nextUrl.clone();
        url.pathname = '/dashboard';
        return NextResponse.redirect(url);
      }

      return supabaseResponse;
    }
    ```

    **Step 7: Create Next.js middleware `src/middleware.ts`**
    ```typescript
    import { type NextRequest } from 'next/server';
    import { updateSession } from '@/lib/supabase/middleware';

    export async function middleware(request: NextRequest) {
      return await updateSession(request);
    }

    export const config = {
      matcher: [
        '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
      ],
    };
    ```
  </action>
  <verify>
    <automated>cd /Users/rouxsparrow/Code/fin-genie && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/00001_initial_schema.sql contains `CREATE TABLE profiles`
    - supabase/migrations/00001_initial_schema.sql contains `household_id UUID NOT NULL DEFAULT`
    - supabase/migrations/00001_initial_schema.sql contains `role TEXT NOT NULL DEFAULT 'viewer' CHECK (role IN ('admin', 'viewer'))`
    - supabase/migrations/00001_initial_schema.sql contains `amount_cents INTEGER NOT NULL` (in transactions table)
    - supabase/migrations/00001_initial_schema.sql contains `ENABLE ROW LEVEL SECURITY` (at least 5 times, one per table)
    - supabase/migrations/00001_initial_schema.sql contains `full_name TEXT NOT NULL` (per D-28)
    - supabase/migrations/00001_initial_schema.sql contains `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()` (per D-29)
    - supabase/migrations/00001_initial_schema.sql contains `update_updated_at_column` trigger function
    - src/lib/types/database.ts contains `export type UserRole = 'admin' | 'viewer'`
    - src/lib/types/database.ts contains `export interface Profile`
    - src/lib/types/database.ts contains `amount_cents: number`
    - src/lib/supabase/client.ts contains `createBrowserClient`
    - src/lib/supabase/server.ts contains `createServerClient`
    - src/lib/supabase/server.ts contains `cookies` import from `next/headers`
    - src/lib/supabase/middleware.ts contains `export async function updateSession`
    - src/lib/supabase/middleware.ts contains redirect to `/login` for unauthenticated users
    - src/lib/supabase/middleware.ts contains redirect to `/dashboard` for authenticated users on `/login`
    - src/middleware.ts contains `import { updateSession }` from supabase middleware
    - src/middleware.ts contains `matcher` config excluding static assets
    - .env.local.example contains `NEXT_PUBLIC_SUPABASE_URL`
    - .env.local.example contains `SUPABASE_SERVICE_ROLE_KEY`
    - TypeScript compilation (`npx tsc --noEmit`) passes without errors
  </acceptance_criteria>
  <done>
    Supabase database schema created with profiles, categories, rules, imports, and transactions tables. All tables have household_id, timestamps, and RLS policies. TypeScript types mirror schema. Browser client, server client, and middleware helper created. Auth middleware redirects unauthenticated users to /login and authenticated users away from /login to /dashboard. Build passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to API | Untrusted user input crosses to server via form submissions and API calls |
| API to Database | Server queries Supabase with user context; RLS enforces row-level access |
| Middleware to Auth | Session tokens in cookies validated on every request |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-01-01 | Spoofing | Auth middleware | High | Mitigate | Middleware calls `supabase.auth.getUser()` on every request -- validates JWT server-side, not just client-side. Unauthenticated users redirected to /login. |
| T-01-02 | Tampering | RLS policies | High | Mitigate | All 5 tables have RLS enabled. Write policies require admin role check via subquery on profiles table. Viewers can only SELECT. |
| T-01-03 | Elevation of Privilege | Role column | High | Mitigate | Role stored in profiles table (server-side), not in JWT claims. RLS policies check role via DB query. Only admins can UPDATE profiles (including role changes). |
| T-01-04 | Information Disclosure | Environment variables | Medium | Mitigate | `.env.local.example` documents required vars. `.env.local` is in .gitignore by default (Next.js). Service role key only used server-side. |
| T-01-05 | Denial of Service | Supabase RLS subqueries | Low | Accept | RLS policies use subqueries on profiles table. For 2-3 household users, performance impact is negligible. Index on household_id mitigates future scaling. |
| T-01-06 | Tampering | Migration SQL | Medium | Mitigate | CHECK constraints on role ('admin'/'viewer') and match_type ('substring'/'regex') columns prevent invalid values at database level. |
</threat_model>

<verification>
1. `npm run build` completes without errors
2. `npx tsc --noEmit` passes without type errors
3. All UI component files exist in `src/components/ui/`
4. globals.css has Neo Brutalism amber theme variables
5. Migration SQL has all 5 tables with RLS enabled
6. Auth utilities export correct functions
</verification>

<success_criteria>
- Next.js project builds and runs with Neo Brutalism amber theme
- All 13 UI components installed (12 neobrutalism + separator)
- DM Sans font configured and loading
- Database schema covers all v1 tables (profiles, categories, rules, imports, transactions)
- RLS policies enforce admin-only writes, authenticated reads
- Auth middleware handles session refresh, login redirects
- TypeScript types match database schema
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
